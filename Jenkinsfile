pipeline {
    agent none
    stages {
        stage('Build') {
            echo "Build Step"
        }
        stage('Test') {
            echo "Build Test"
        }
        stage('Deliver') {
            echo "Build Deliver"
        }
    }
}
